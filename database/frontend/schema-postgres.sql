-- Frontend does not require direct DB access.
-- Kept for symmetry in repo structure; we place the same backend schema here
-- to simplify provisioning when deploying infra from a single path.
\i ../backend/schema-postgres.sql


