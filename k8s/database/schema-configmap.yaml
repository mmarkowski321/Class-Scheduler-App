apiVersion: v1
kind: ConfigMap
metadata:
  name: eduscheduler-schema
data:
  schema.sql: |
    -- Embedded schema for initialization (short form). For full schema, mount from repo if needed.
    -- You can regenerate this ConfigMap with the complete file if it changes.
    -- BEGIN
    \echo 'Applying EduScheduler schema...'
    -- Ensure extensions/functions used by triggers are available
    -- (plpgsql is installed by default)
    -- Create objects
    \ir /dev/stdin
    -- END



