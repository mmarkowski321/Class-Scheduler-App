apiVersion: v1
kind: Service
metadata:
  name: eduscheduler-frontend
spec:
  type: NodePort
  selector:
    app: eduscheduler-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080


